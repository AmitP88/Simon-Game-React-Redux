import React, { Component } from 'react';

class LevelCounter extends Component {
    render() {
        return (
            <div className="LevelCounter-container"></div>
        );
    }
}

export default LevelCounter;